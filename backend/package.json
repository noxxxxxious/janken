{
	"type": "module",
	"scripts": {
		"start": "rm -rf ./out && tsc && cp -r ./src/public ./out && node ./out/main.js",
		"nodemon-start": "nodemon --watch /src --exec pnpm start"
	},
	"dependencies": {
		"dotenv": "^16.3.1",
		"express": "^4.18.2",
		"mongoose": "^7.3.1",
		"socket.io": "^4.7.0"
	},
	"devDependencies": {
		"@types/express": "^4.17.17",
		"nodemon": "^2.0.22"
	}
}
